# L3 Catalog Placement

v0.0.3rc04 carries forward the first seven published SCAF L3 Pattern / Mechanism entries and performs only the localized trace cleanup required by the independent v0.0.3rc03 tranche review.

Family paths are:

```text
catalog/SUP/
catalog/COM/
catalog/REC/
catalog/FTL/
catalog/TIM/
catalog/PST/
catalog/LCM/
catalog/EVD/
catalog/SYN/
catalog/SEC/
```

Only families with current entries require committed directories. Pattern identity is based on the immutable primary mechanism family after publication. Cross-concern coverage belongs in pattern metadata through L2 trace relations rather than by duplicating the same pattern under multiple family paths.

All seven entries remain `Candidate / M1` in v0.0.3rc04; publication of the ID does not imply `Available` status, project selection or L2 satisfaction.
